<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Salut <%= "#{@user.first_name} #{@user.last_name}" %> !</h1>
    <p>
      Tu t'es inscrit à l'évènement "<%= "#{@event.title}" %>"
      Il aura lieu le <%= "#{@event.start_date}" %>, et durera <%= "#{@event.duration}" %>!
    </p>
    <p>
      Pour accéder à ton espace client, connecte-toi via : <%= @url %>.
    </p>
    <p> 
      À très vite sur the_event_project.fr ! 
    </p>
  </body>
</html>